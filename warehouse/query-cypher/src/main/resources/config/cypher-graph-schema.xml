<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
                           https://www.springframework.org/schema/beans/spring-beans.xsd">

	<!--
	    M1 graph-schema for the TVMAZE fixture. Maps the Cypher Actor label to
	    the on-disk PERSON vertices that the TV_COSTARS edge definition writes
	    from EMBEDDED_CAST_PERSON_NAME values, and the Cypher COSTAR_OF rel
	    type to the TV_COSTARS edges (which are bidirectional, hence
	    UNDIRECTED here).

	    M2 will add additional labels/properties (Show, Network) once shard
	    enrichment lands.

	    Constructor args are positional (matches NodeMapping/RelMapping
	    constructor signatures); the project does not compile with
	    '-parameters', so name-based binding is not reliable.
	-->

	<bean id="actorNode" class="datawave.query.cypher.mapping.NodeMapping">
		<!-- label -->
		<constructor-arg index="0" value="Actor" />
		<!-- dataType -->
		<constructor-arg index="1" value="tvmaze" />
		<!-- identityProperty -->
		<constructor-arg index="2" value="name" />
		<!-- identityField -->
		<constructor-arg index="3" value="EMBEDDED_CAST_PERSON_NAME" />
		<!-- properties (cypher-name -> on-disk-field) -->
		<constructor-arg index="4">
			<map key-type="java.lang.String" value-type="java.lang.String">
				<entry key="name" value="EMBEDDED_CAST_PERSON_NAME" />
				<entry key="id" value="EMBEDDED_CAST_PERSON_ID" />
			</map>
		</constructor-arg>
	</bean>

	<bean id="costarOfRel" class="datawave.query.cypher.mapping.RelMapping">
		<!-- cypherType -->
		<constructor-arg index="0" value="COSTAR_OF" />
		<!-- edgeType -->
		<constructor-arg index="1" value="TV_COSTARS" />
		<!-- sourceLabel -->
		<constructor-arg index="2" value="Actor" />
		<!-- sinkLabel -->
		<constructor-arg index="3" value="Actor" />
		<!-- direction -->
		<constructor-arg index="4" value="UNDIRECTED" />
		<!-- attributeMappings (cypher-property -> EdgeAttributeSlot) -->
		<constructor-arg index="5">
			<map key-type="java.lang.String" value-type="datawave.query.cypher.mapping.EdgeAttributeSlot">
				<entry key="show" value="ATTRIBUTE2" />
				<entry key="showId" value="ATTRIBUTE3" />
			</map>
		</constructor-arg>
	</bean>

</beans>
